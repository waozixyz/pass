- Generate passwords that match LessPass clients for the same site, login, master password, counter, character classes, and length settings.
- Limit password length to the LessPass-compatible 5 to 35 character range.
