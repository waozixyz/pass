- Generate passwords that match LessPass clients for the same site, login, master password, counter, character classes, and length settings.
- Limit password length to the LessPass-compatible 5 to 35 character range.
- Require Android fingerprint unlock for saved master passwords and store them with an authenticated Android Keystore key.
- Use the Sweet Material theme by default and show the generated password in a dedicated output field.
